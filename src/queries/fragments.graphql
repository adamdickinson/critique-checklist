fragment ClientInfo on Client {
  id
  name
}

fragment FeedbackInfo on Feedback {
  id
  summary
  detail
  completed
  with
  roundId
}

fragment FeedbackRevisionInfo on FeedbackRevision {
  id
  createdAt
  feedbackId
  summary
  detail
}

fragment ProjectInfo on Project {
  id
  name
  clientId
}

fragment RoundInfo on Round {
  id
  number
  projectId
  active
  openAt
  closedAt
}

